## 智慧农业监测系统 · 前端

开发与构建说明见仓库根目录 [README.md](../README.md)。

```bash
cd ruoyi-ui
npm install
npm run dev
```

默认开发地址：http://localhost:80（端口以 `vue.config.js` / 环境变量为准）。

```bash
npm run build:stage
npm run build:prod
```
